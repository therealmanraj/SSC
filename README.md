# SSC

1. Paste the excel file in the directory

2. Create a .env file in the directory and add the password

PASSWORD="like_this"

3. Create a virtual env and install the required packages i.e requirements.txt

4. Run extract_csv.py to extract all the data from all the tabs, it keeps each tab data (CSVs) into data folder
